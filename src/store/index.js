import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    user:null  // an Object store user infomation(token...)
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
